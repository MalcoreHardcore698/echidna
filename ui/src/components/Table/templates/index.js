import categoryTemplate from './category'
import articleTemplate from './article'
import projectTemplate from './project'
import ticketTemplate from './ticket'
import roleTemplate from './role'
import userTemplate from './user'

export const category = categoryTemplate
export const article = articleTemplate
export const project = projectTemplate
export const ticket = ticketTemplate
export const role = roleTemplate
export const user = userTemplate

export default { category, article, project, ticket, role, user }
