import React from 'react'
import { storiesOf } from '@storybook/react'
import Pagination from '.'

storiesOf('Components API/Navigation/Pagination', module).add('Default', () => {
  return <Pagination />
})
